# ============================================
# LVCampusConnect System - PRODUCTION Configuration Example
# ============================================
# INSTRUCTIONS:
# 1. Copy this file to .env.production: cp .env.production.example .env.production
# 2. Fill in your actual values (DO NOT commit .env.production file!)
# 3. This file is used automatically when deploying to Cloudflare Pages

# Google OAuth Configuration
VITE_GOOGLE_CLIENT_ID=your_google_client_id_here

# ============================================
# Backend API URLs - PRODUCTION Hybrid Architecture
# ============================================

# Cloud Backend (Production) - Used by Admin in production
VITE_CLOUD_BACKEND_URL=https://lvcampusconnect-backend.onrender.com

# Local Backend - Used by Kiosk for printing (on kiosk machines)
VITE_LOCAL_BACKEND_URL=http://localhost:5000

# Legacy API URL (for backward compatibility)
VITE_API_URL=https://lvcampusconnect-backend.onrender.com

# ============================================
# Environment Configuration
# ============================================

# PRODUCTION MODE: Admin uses CLOUD backend, Kiosk uses LOCAL backend
VITE_NODE_ENV=production

# ============================================
# Usage Notes:
# ============================================
# - This file is used during production builds (npm run build)
# - Admin operations will use CLOUD backend (Render.com)
# - Kiosk operations will use LOCAL backend (for thermal printing)
# - Both backends connect to the same MongoDB Atlas database

