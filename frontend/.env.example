# ============================================
# LVCampusConnect System - Frontend Configuration Example
# ============================================
# INSTRUCTIONS:
# 1. Copy this file to .env: cp .env.example .env
# 2. Fill in your actual values (DO NOT commit .env file!)
# 3. For production, values will be read from .env.production

# Google OAuth Configuration
# Get your client ID from: https://console.cloud.google.com/apis/credentials
# Add authorized origins: http://localhost:5173 and https://lv-campus-connect.pages.dev
VITE_GOOGLE_CLIENT_ID=your_google_client_id_here

# ============================================
# Backend API URLs - Hybrid Architecture
# ============================================

# Cloud Backend (Production) - Used by Admin in PRODUCTION
# This is the backend deployed on Render
VITE_CLOUD_BACKEND_URL=https://lvcampusconnect-backend.onrender.com

# Local Backend (Development & Kiosk) - Used for local development and kiosk printing
# This runs locally on the kiosk machine or your development machine
VITE_LOCAL_BACKEND_URL=http://localhost:5000

# Legacy API URL (for backward compatibility)
VITE_API_URL=http://localhost:5000

# ============================================
# Environment Configuration
# ============================================

# Environment mode (development/production)
# IMPORTANT: Set to 'development' for local testing, 'production' for deployment
# When 'development': ALL operations (admin + kiosk) use LOCAL backend (localhost:5000)
# When 'production': Admin uses CLOUD backend, Kiosk uses LOCAL backend
VITE_NODE_ENV=development

# ============================================
# Usage Notes:
# ============================================
# DEVELOPMENT MODE (VITE_NODE_ENV=development):
# - ALL operations (admin + kiosk) use LOCAL backend (localhost:5000)
# - This allows you to test everything locally before deploying
# - Make sure your local backend is running: npm run dev (in backend folder)
# - Changes can be tested immediately without deployment
#
# PRODUCTION MODE (VITE_NODE_ENV=production):
# - Admin operations use CLOUD backend (Render.com)
# - Kiosk operations use LOCAL backend (for printing)
# - Both backends connect to the same MongoDB Atlas database
# - Data remains consistent across all instances
#
# TO SWITCH TO PRODUCTION MODE:
# - Change VITE_NODE_ENV to 'production'
# - Or deploy to Cloudflare Pages (automatically uses .env.production)

